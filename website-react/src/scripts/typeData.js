class TypeData {
    constructor(typeClass, typeName, typeIconPath) {
        this.typeClass = typeClass;
        this.typeName = typeName;
        this.typeIconPath = typeIconPath;
    }
}

export default TypeData;